#main(args)->() {
 || x=[2, 5, 6], filter(x)->r, print(r);
}

#filter(arr)->(r) {
  arr=list(a, b), b=list(c,k) || raw(c)->r;
                 :
                 || r<-5
}

#raw(v)->(r) {
    v < 10 || r <- v;
           :
           || r<-100
}