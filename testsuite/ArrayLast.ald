#main(args)->() {
 || x=[2, 5, 6], last(x)->r, print(r);
}

#last(arr)->(r) {
  arr=null || r<-0;
  arr=list(a, b), b=null || r<-a;
  arr=list(a, b), b=list(c,d) || last(b)->r;
                 :
                 || r<-555
}