#main(args)->() {
 || x=[2, 5, 6, 7, 8, 24, 21], last(x)->r, print(last(x));
}

#last(arr)->(r) {
  arr=list(a, b), b=empty     || double(a)->p, r=[a, 6, 7, p];
  arr=list(a, b), b=list(c,d) || last(b)->r;
                              :
                              || r<-0
}

#double(x)->y {
      x>0       || y<-x+x;
                :
                || y<-x+x
}