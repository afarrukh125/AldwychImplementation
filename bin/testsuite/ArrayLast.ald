#main(args)->() {
 || x=[2, 5, 6, 7, 8, 24, 21], last(x)->r, print(r);
}

#last(arr)->(r) {
  arr=list(a, b), b=empty()   || r<-a;
  arr=list(a, b), b=list(c,d) || last(b)->r;
                              :
                              || r<-0
}